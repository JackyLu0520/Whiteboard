My Whiteboard Tool

Run it:
pip3 install pygame #Install pygame.If you had installed it before,you don't have to run the command.
pip3 install easygui #Install easygui.If you had installed it before,you don't have to run the command.
python3 /path/to/Whiteboard.pygame  #Run it!